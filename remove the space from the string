#include <stdio.h>
#include <string.h>
#include<ctype.h>
int main()
{
    char a[100];
    int i=0;
    fgets(a,100,stdin);
    int len=strlen(a);
    for(int i=0;i<len;i++){
            if(a[i]!=' ')
            printf("%c",a[i]);
    }
    return 0;
}
