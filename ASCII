#include <stdio.h>
#include<ctype.h>
int main()
{
    char a[100];
    int i=0;
    fgets(a,100,stdin);
    while(a[i]!='\0'){
        if(isalpha (a[i])){
        printf("%d ",a[i]-64);
      }
      i++;
    }
    
    return 0;
}
