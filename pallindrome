#include <stdio.h>
#include <string.h>
#include<ctype.h>
int main()
{
    char a[100];
    int i=0;
    fgets(a,100,stdin);
    int len=strlen(a);
    for(int i=0;i<len-1;i++){
        //for(int j=strlen(a)-1;j!=i;j--){
            if(a[i]!=a[len-2-i])
            printf("nap ");
            //return 0;
        //}
    }
    printf("p");
    return 0;
}
